#include "binary_trees.h"

/**
* binary_tree_is_root -  checks if a given node is a root.
* @node: node will be check.
* Return: 1 or 0.
*/
int binary_tree_is_root(const binary_tree_t *node)
{
	if (node)
	if (node-> parent == NULL)
	return (1);
	else
	reurn (0);
	}
	return (0);
}
